*.o
.DS_Store
.*.swp
/env
/dl
/.config
/.config.old
/.toolchain_build_ver
/bin
/build_dir
/staging_dir
/tmp
/logs
/feeds
/feeds.conf
/files
/target/linux/feeds
/overlay
/package/feeds
/package/openwrt-packages
/feeds-local
/.build-deps
/.config-x3000
/.x3000-variant
/bin-x3000-*
/x3000-build*.log

# files-private/ is the per-builder slot for stuff that should ship in
# the private rootfs but stay out of the public repo (internal CA,
# feed-signing pubkey, etc.). Only .gitkeep is tracked so the directory
# survives in git; everything else is local-only.
/x3000/files-private/*
!/x3000/files-private/.gitkeep

# Per-builder additions for the private build: extra package repos
# (custom-feeds.private.local) and extra config knobs
# (config.private.local). Both gitignored so each builder's private
# package selection stays out of the public repo.
/x3000/custom-feeds.*.local
/x3000/config.*.local
/*.patch
/llvm-bpf*
key-build*
private-key.pem
public-key.pem
*.orig
*.rej
*~
.#*
*#
.emacs.desktop*
TAGS*~
git-src
.project
.cproject
.ccache
.vscode*

# Cowork desktop app drops delivered files here (inside the connected
# folder); never track them.
Claude outputs/
